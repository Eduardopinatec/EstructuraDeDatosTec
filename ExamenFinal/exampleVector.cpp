// constructing vectors
#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main ()
{
  // constructors used in the same order as described above:
    vector<string> vect;
    vect.push_back("lalo");
    vect.push_back("lorenzo");
    vect.push_back("fer");
  return 0;
}